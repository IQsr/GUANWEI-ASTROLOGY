# 觀微 —— 環境變數樣板
#
# 複製做 apps/web/.env.local（或者 repo 根目錄 .env.local）再填。
# .gitignore 已經擋住 .env*.local —— 唔好將真值 commit 入嚟。
#
# ⚠ 冇呢幾條都行得：私密層（書齋、命書、認領）會顯示「一時搵唔到」，
#    公開層（入齋、藏經閣）同排盤、題名、展卷全部照行。

# 站點絕對網址（canonical、OG、sitemap、robots 全部靠佢）
#
# ⚠ Vercel 上面唔入呢條，全站 canonical、37 條 sitemap 同 robots.txt
#    會一律指住 http://localhost:3000 —— 而藏經閣係唯一流量入口。
#    本機開發唔使填（lib/site.ts 會 fallback 去 localhost:3000）。
NEXT_PUBLIC_SITE_URL=

# Supabase project settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ⚠⚠ service role key bypass 晒 RLS。
#    唔好貼落任何對話、issue、screenshot 度；唔好放喺 NEXT_PUBLIC_ 開頭嘅變數。
#    lib/env.ts 會擋住「貼錯咗入 anon key」嗰條路，
#    scripts/check-bundle.mjs 會喺佢出現喺 client bundle 嗰陣 fail 個 build。
#    暫時未有 server 用得著佢（G3 Stripe webhook 先要），可以唔填。
SUPABASE_SERVICE_ROLE_KEY=
